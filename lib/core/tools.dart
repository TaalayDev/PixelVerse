enum PixelTool {
  pencil,
  fill,
  eraser,
  line,
  rectangle,
  circle,
  select,
  eyedropper,
  brush,
  mirror,
  gradient,
  rotate,
  pixelPerfectLine,
  sprayPaint,
  drag,
  contour,
  pen,
}

enum MirrorAxis { horizontal, vertical, both }
